type: base
build:
  auto_version: true
  architectures:
    - amd64
  variants:
    - tag: "15"
      args:
        BASE_VERSION: "15-pkg"
      aliases: ["latest", "15-quarterly"]
    - tag: "15-latest"
      args:
        BASE_VERSION: "15-pkg-latest"

cit:
  mode: shell
